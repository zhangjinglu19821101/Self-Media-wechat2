// Agent 必须返回的标准结构（纯文本，无HTML）
export interface WechatArticle {
  title: string;        // 标题
  author: string;       // 作者
  date: string;         // 日期 2026-03-30
  content: string;      // 纯文本正文：加粗【】、引用>、换行\n
  digest?: string;      // 摘要
  thumbMediaId?: string;// 封面ID（上传后获取）
}