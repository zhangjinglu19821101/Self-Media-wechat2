# 代码执行涉及文件和打印情况说明

---

## 📁 涉及的文件清单

### 1. 测试 API 文件（3个）

| 文件路径 | 用途 | 状态 |
|---------|------|------|
| `src/app/api/test/start-from-json/route.ts` | **推荐使用** - 直接从 JSON 开始测试 | ✅ 最新 |
| `src/app/api/test/trigger-step6/route.ts` | 触发第六步测试 | ✅ 可用 |
| `src/app/api/test/step5-to-step9/route.ts` | 直接测试工程代码（已过时） | ❌ 不推荐 |

---

### 2. 修改的核心文件（1个）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/lib/services/subtask-execution-engine.ts` | 将 6 个关键方法从 `private` 改为 `public` |

**改为 public 的方法**：
1. `callExecutorAgent()` - 调用执行Agent
2. `queryCapabilityList()` - 查询 capability_list
3. `callAgentB()` - 调用 Agent B
4. `executeCapability()` - 执行 capability
5. `sendBackToExecutor()` - 返回给执行Agent
6. `markTaskCompleted()` - 保存最终结果

---

### 3. 依赖的核心文件（多个）

| 文件路径 | 用途 |
|---------|------|
| `src/lib/db/schema.ts` | 数据库 schema 定义 |
| `src/lib/db/index.ts` | 数据库连接 |
| `src/lib/agent-llm.ts` | LLM 调用封装 |
| `src/lib/mcp/branch1-intelligent-executor.ts` | 分支1智能执行器 |
| `src/lib/types/capability-types.ts` | 能力类型定义 |
| `src/lib/types/interact-content.ts` | 交互内容类型定义 |

---

## 🖨️ 关键信息打印情况

### 推荐使用：`start-from-json/route.ts`

#### ✅ 1. 打印请求 Agent B 的提示词

**位置**：第 128-130 行

```typescript
console.log('📤 [第五步后半部分][出口结果] Agent B 提示词构建完成');
console.log('📋 提示词长度:', agentBPrompt.length, '字符');
console.log('📋 提示词完整内容:\n', agentBPrompt);
```

**打印内容**：
- ✅ 提示词长度
- ✅ **提示词完整内容**（最重要！）

---

#### ✅ 2. 打印 Agent B 返回的需要调用 MCP 的功能的相关参数

**位置 1**：第 155-158 行（原始响应）

```typescript
console.log('📤 [第六步][出口结果] Agent B 原始响应:', response);
```

**打印内容**：
- ✅ Agent B 的原始响应（未解析的字符串）

---

**位置 2**：第 172-174 行（解析成功）或第 197-199 行（Mock 数据）

```typescript
console.log('✅ [第六步][最终结果] Agent B 输出:', JSON.stringify(agentBOutput, null, 2));
```

**打印内容**：
- ✅ `solutionNum` - 选定的方案ID
- ✅ `toolName` - 工具名
- ✅ `actionName` - 方法名
- ✅ `params` - MCP 调用参数（最重要！）
- ✅ `reasoning` - 选择理由

---

#### ✅ 3. 其他关键打印

| 步骤 | 打印内容 | 位置 |
|------|---------|------|
| **步骤 0** | 输入 JSON（完整） | 第 33 行 |
| **第五步** | 查询 capability_list 的入口参数 | 第 51 行 |
| **第五步** | 查询 capability_list 的出口结果 | 第 64-67 行 |
| **第五步** | 能力列表详情（含 param_desc） | 第 68-74 行 |

---

### 📊 完整的打印日志示例

当你调用 `start-from-json` API 时，会看到以下打印：

```
🧪 ========== 直接从 JSON 开始测试 ==========
📋 目标：从执行 agent 的 JSON 输出开始，直接测试 Step 5 to Step 6

========== 📥 步骤 0：读取输入 JSON ==========
📥 [步骤 0][入口参数] 输入 JSON: {
  "isNeedMcp": true,
  "problem": "无法上传微信公众号文章草稿...",
  "capabilityType": "platform_publish"
}

========== 🔍 第五步：查询 capability_list 表 ==========
📥 [第五步][入口参数] 查询 capability_list: {
  status: 'available',
  capabilityType: 'platform_publish'
}
📤 [第五步][出口结果] 找到 2 个可用能力
📋 能力列表: [
  { id: 11, functionDesc: '微信公众号-添加草稿', ... },
  { id: 14, functionDesc: '微信公众号-上传图片素材', ... }
]

========== 📝 第五步后半部分：构建 Agent B 提示词 ==========
📤 [第五步后半部分][出口结果] Agent B 提示词构建完成
📋 提示词长度: 2847 字符
📋 提示词完整内容:
 你是 Agent B，负责解决方案选型和 MCP 参数生成。
  
  【执行 agent 反馈的问题】
  {
    "isNeedMcp": true,
    "problem": "无法上传微信公众号文章草稿...",
    "capabilityType": "platform_publish"
  }
  
  【系统可用的 MCP 能力清单】
  能力 ID: 11
  功能描述: 微信公众号-添加草稿
  ...（完整提示词）...

========== 🤖 第六步：调用 Agent B ==========
📥 [第六步][入口参数] 调用 callLLM
  - agent: agent B
  - task: 解决方案选型 + MCP 参数生成
📤 [第六步][出口结果] Agent B 原始响应: {
  "solutionNum": 11,
  "toolName": "wechat",
  "actionName": "addDraft",
  "params": { ... },
  "reasoning": "..."
}
✅ [第六步][最终结果] Agent B 输出: {
  "solutionNum": 11,
  "toolName": "wechat",
  "actionName": "addDraft",
  "params": {
    "accountId": "insurance-account",
    "articles": [
      {
        "title": "测试文章...",
        "author": "保险事业部",
        "digest": "...",
        "content": "...",
        "show_cover_pic": 0
      }
    ]
  },
  "reasoning": "选择 ID=11 方案..."
}

🎉 ========== 测试完成 ==========
```

---

## 🎯 使用建议

### 推荐测试命令

```bash
curl -X POST http://localhost:5000/api/test/start-from-json \
  -H "Content-Type: application/json" \
  -d '{
    "isNeedMcp": true,
    "problem": "无法上传微信公众号文章草稿，任务执行阻塞，缺少平台发布能力",
    "capabilityType": "platform_publish"
  }'
```

### 查看打印日志

测试时，查看服务器控制台输出，会看到：
1. ✅ 输入 JSON
2. ✅ 查询 capability_list 的结果
3. ✅ **Agent B 的完整提示词**（最重要！）
4. ✅ Agent B 的原始响应
5. ✅ **Agent B 输出的完整 MCP 调用参数**（最重要！）

---

## 📝 总结

| 问题 | 答案 |
|------|------|
| **涉及的文件** | 见上方清单 |
| **打印请求 Agent B 的提示词了吗？** | ✅ 是的！完整打印！ |
| **打印 Agent B 返回的 MCP 参数了吗？** | ✅ 是的！完整打印！ |

**两个最重要的信息都完整打印了！** 🎉
