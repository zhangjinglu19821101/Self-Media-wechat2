# 超简单解释：TOOL_MAP["wechat"]["addDraft"] 到底在做什么？

## 📝 实际代码（从项目中提取）

### 1. TOOL_MAP 的定义（实际代码）

```typescript
// 文件：src/lib/mcp/generic-mcp-call.ts

import { SearchMCPTools } from './search-tools';
import { WechatMCPTools } from './wechat-tools';

// 👇 就是这个！
const TOOL_MAP: Record<string, any> = {
  search: SearchMCPTools,    // ← key 是 "search"，value 是 SearchMCPTools 对象
  wechat: WechatMCPTools,    // ← key 是 "wechat"，value 是 WechatMCPTools 对象
};
```

---

### 2. WechatMCPTools 的定义（实际代码）

```typescript
// 文件：src/lib/mcp/wechat-tools.ts

// 底层函数（带前缀，避免冲突）
export async function wechatAddDraft(params) { ... }
export async function wechatUploadMedia(params) { ... }

// 👇 就是这个！
export const WechatMCPTools = {
  addDraft: wechatAddDraft,      // ← key 是 "addDraft"，value 是 wechatAddDraft 函数
  uploadMedia: wechatUploadMedia, // ← key 是 "uploadMedia"，value 是 wechatUploadMedia 函数
  getAccounts: wechatGetAccounts, // ← key 是 "getAccounts"，value 是 wechatGetAccounts 函数
};
```

---

## 🎯 现在一步步看：`TOOL_MAP["wechat"]["addDraft"]`

### 第一步：`TOOL_MAP["wechat"]`

```typescript
const TOOL_MAP = {
  search: SearchMCPTools,
  wechat: WechatMCPTools,  // ← 取这个！
};

const tool = TOOL_MAP["wechat"];
// 结果：tool = WechatMCPTools 对象
//      也就是：{ addDraft: wechatAddDraft, uploadMedia: wechatUploadMedia, ... }
```

---

### 第二步：`tool["addDraft"]`

```typescript
// 刚才得到：tool = WechatMCPTools
// 也就是：
const tool = {
  addDraft: wechatAddDraft,      // ← 取这个！
  uploadMedia: wechatUploadMedia,
  getAccounts: wechatGetAccounts,
};

const method = tool["addDraft"];
// 结果：method = wechatAddDraft 函数！
//      就是那个底层的函数！
```

---

### 第三步：调用它！

```typescript
// 刚才得到：method = wechatAddDraft
// 直接调用！
const result = await method(params);
// 实际上就是：await wechatAddDraft(params)
```

---

## 📊 完整流程图（用实际代码）

```
┌─────────────────────────────────────────────────────────────┐
│  1. 从 capability_list 读取                                   │
│     tool_name = "wechat"                                      │
│     action_name = "addDraft"                                  │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  2. TOOL_MAP["wechat"]                                        │
│                                                               │
│     const TOOL_MAP = {                                        │
│       search: SearchMCPTools,                                 │
│       wechat: WechatMCPTools,  ← 找到这个！                  │
│     };                                                         │
│                                                               │
│     结果：WechatMCPTools 对象                                 │
│          {                                                     │
│            addDraft: wechatAddDraft,                          │
│            uploadMedia: wechatUploadMedia,                     │
│            ...                                                 │
│          }                                                     │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  3. WechatMCPTools["addDraft"]                                │
│                                                               │
│     const WechatMCPTools = {                                  │
│       addDraft: wechatAddDraft,  ← 找到这个！                │
│       uploadMedia: wechatUploadMedia,                         │
│       ...                                                     │
│     };                                                         │
│                                                               │
│     结果：wechatAddDraft 函数（底层实现！）                   │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  4. 调用它！                                                   │
│     await wechatAddDraft(params)                               │
│                                                               │
│     ✅ 完成！                                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 💻 实际代码演示（复制就能运行）

```typescript
// 模拟代码，你可以复制到浏览器控制台试试！

// 1. 定义底层函数
function wechatAddDraft(params) {
  console.log('执行 wechatAddDraft，参数:', params);
  return { success: true, media_id: '123456' };
}

function wechatUploadMedia(params) {
  console.log('执行 wechatUploadMedia，参数:', params);
  return { success: true, media_id: '789012' };
}

// 2. 定义 WechatMCPTools 对象
const WechatMCPTools = {
  addDraft: wechatAddDraft,
  uploadMedia: wechatUploadMedia,
};

// 3. 定义 TOOL_MAP
const TOOL_MAP = {
  wechat: WechatMCPTools,
};

// 4. 现在模拟调用！
const tool_name = "wechat";
const action_name = "addDraft";
const params = { title: "测试文章" };

// 👇 就是这两步！
const tool = TOOL_MAP[tool_name];           // ← 第一步
const method = tool[action_name];            // ← 第二步
const result = method(params);                // ← 第三步：调用！

console.log('结果:', result);
```

---

## 🎯 总结（一句话）

```
TOOL_MAP["wechat"]["addDraft"] 
    ↓
先找到 TOOL_MAP["wechat"] = WechatMCPTools 对象
    ↓
再找到 WechatMCPTools["addDraft"] = wechatAddDraft 函数
    ↓
然后调用它！
```

---

**现在看懂了吗？** 😊
